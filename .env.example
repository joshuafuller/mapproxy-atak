# Docker listens on every host interface so ATAK devices on the LAN can connect.
MAPPROXY_BIND_ADDRESS=0.0.0.0
MAPPROXY_PORT=8080
PUBLIC_PORT=8080

# Optional Grafana dashboard. Keep loopback-only unless the LAN is trusted.
GRAFANA_BIND_ADDRESS=127.0.0.1
GRAFANA_PORT=3000
